apiVersion: apps/v1
kind: Deployment
metadata:
  name: alert-service
  namespace: tradevision
  labels:
    app: alert-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: alert-service
  template:
    metadata:
      labels:
        app: alert-service
    spec:
      containers:
        - name: alert-service
          image: tradevision/alert-service:latest
          ports:
            - containerPort: 8085
          envFrom:
            - configMapRef:
                name: tradevision-config
          env:
            - name: DB_NAME
              value: "alertdb"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: tradevision-secrets
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: tradevision-secrets
                  key: JWT_SECRET
            - name: PORT
              value: "8085"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: alert-service
  namespace: tradevision
spec:
  selector:
    app: alert-service
  ports:
    - port: 8085
      targetPort: 8085
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: alert-service-hpa
  namespace: tradevision
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: alert-service
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
