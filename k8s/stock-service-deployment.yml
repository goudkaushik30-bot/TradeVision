apiVersion: apps/v1
kind: Deployment
metadata:
  name: stock-service
  namespace: tradevision
  labels:
    app: stock-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: stock-service
  template:
    metadata:
      labels:
        app: stock-service
    spec:
      containers:
        - name: stock-service
          image: tradevision/stock-service:latest
          ports:
            - containerPort: 8082
          envFrom:
            - configMapRef:
                name: tradevision-config
          env:
            - name: DB_NAME
              value: "stockdb"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: tradevision-secrets
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: tradevision-secrets
                  key: JWT_SECRET
            - name: PORT
              value: "8082"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "1000m"
---
apiVersion: v1
kind: Service
metadata:
  name: stock-service
  namespace: tradevision
spec:
  selector:
    app: stock-service
  ports:
    - port: 8082
      targetPort: 8082
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: stock-service-hpa
  namespace: tradevision
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: stock-service
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
